export enum RoutesEnum {
  LANDINGPAGE = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  REGISTERCONFIRM = "/register-confirm",
  MYPROFILE = "/my-profile"
}

export enum AccountTypes {
  LANDLORD = "landlord",
  ADMIN = "admin",
  USER = "user"
}
