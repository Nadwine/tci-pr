export const listingStatusSequence = ["awaiting approval", "approved", "in offer", "gone"];
export type ListingStatusEnum = "awaiting approval" | "approved" | "in offer" | "gone";

export const tenancyStatusSequence = ["generating-documents", "awaiting-signatures", "ongoing", "ended"];
export type TenancyStatusEnum = "generating-documents" | "awaiting-signatures" | "ongoing" | "ended";
