import PropTypes from "prop-types";
import React from "react";
import { connect } from "react-redux";

export const TenantAndProperty = props => {
  return <div>TenantAndProperty</div>;
};

export default connect()(TenantAndProperty);
