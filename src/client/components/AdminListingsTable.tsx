import PropTypes from "prop-types";
import React from "react";
import { connect } from "react-redux";

export const AdminListingsTable = props => {
  return <div>AdminListingsTable</div>;
};

export default connect()(AdminListingsTable);
